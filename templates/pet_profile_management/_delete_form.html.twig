<form method="post" action="{{ path('app_pet_profile_management_delete', {'id': pet_profile_management.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pet_profile_management.id) }}">
    <button class="btn">Delete</button>
</form>
