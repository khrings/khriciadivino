{% extends '_layouts/master.html.twig' %}

{% block title %}New PetProfileManagement{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-4">
   <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold text-gray-800">Create New Pet Profile</h1>
        <a href="{{ path('app_pet_profile_management_index') }}" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg transition duration-200 flex items-center gap-2 shadow-md">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
            </svg>
            Back to List
        </a>
    </div>

    <div class="bg-white rounded-lg shadow-md p-8">
        {{ include('pet_profile_management/_form.html.twig') }}
    </div>
</div>
{% endblock %}
