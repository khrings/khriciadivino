<form method="post" action="{{ path('app_productss_delete', {'id': productss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ productss.id) }}">
    <button class="btn">Delete</button>
</form>
